(function (app) {
    app.controller('homeController', homeController);

    homeController.$inject = ['$state', '$window'];

    function homeController($state, $window) {
        
    }
})(angular.module('nbtapp'));